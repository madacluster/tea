pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'docker ps'
      }
    }
    stage('') {
      steps {
        echo 'hola'
      }
    }
  }
  environment {
    casa = 'true'
  }
}